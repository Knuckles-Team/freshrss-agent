[pytest]
timeout = 60
asyncio_mode = auto
testpaths = tests
markers =
    integration: Integration tests
    concept(id): Trace a test to a CONCEPT:FRSS-* registry entry for traceability
addopts = -m "not integration"
filterwarnings =
    ignore:.*exclude_args.*
